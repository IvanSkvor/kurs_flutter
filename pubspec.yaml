name: taskmaster
description: "Task Manager App"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  table_calendar: ^3.0.9  # Добавьте эту строку
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3 # Добавляем эту строку
flutter:
  uses-material-design: true